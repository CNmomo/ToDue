import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Pool = () => {
  return (
    <View>
      <Text>Pool</Text>
    </View>
  );
}

const styles = StyleSheet.create({})

export default Pool;
